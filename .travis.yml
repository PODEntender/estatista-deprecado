install:
  - npm install

script:
  - npm run build
  - npm run clean

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  verbose: true
  target-branch: gh-pages
  on:
    branch: master
